{
  "name": "@carry-or-fall/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phaser 4 browser client (Vite). Boots into the local PlayScene: fixed-step movement and map collision, no server required. BootScene (build info + live server connection status) is retained for the M4 networked entry point.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@carry-or-fall/protocol": "workspace:*",
    "@carry-or-fall/simulation-core": "workspace:*",
    "@colyseus/sdk": "0.17.43",
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vite": "8.1.5"
  }
}
