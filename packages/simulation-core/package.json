{
  "name": "@carry-or-fall/simulation-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, fixed-step simulation. Hosted by the authoritative Colyseus room since M4: one world holds two to eight players, advanced 50ms at a time from their validated inputs. Ships a seeded PRNG, movement, collision, the shared attack pipeline with the hard caps, enemies, inventory and the secure slot, skills, extraction, and run results.",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@carry-or-fall/game-content": "workspace:*"
  }
}
